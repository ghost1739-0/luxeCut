# Maison Barber — Proje Notları

Next.js değil, **TanStack Start** (Vite tabanlı) + Supabase kullanan bir randevu sistemi.

- `src/routes/` — sayfalar ve API route'ları (dosya tabanlı yönlendirme)
- `src/integrations/supabase/` — Supabase client'ları (browser + server)
- `supabase/migrations/` — veritabanı şeması (SQL)
